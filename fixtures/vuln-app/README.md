# vuln-app (test fixture)

A deliberately weak, NON-FUNCTIONAL toy app used as the Soundcheck scanner test
target. Every "secret" in here is a documented fake example value (e.g. AWS's
official docs example key). Do not deploy; do not reuse any pattern from here.
