# Soundcheck — FULL container deps (BFF + agents + scanners) for the Render host.
# (Vercel uses backend/requirements.txt, which is just the read-only BFF subset.)
fastapi==0.136.3
uvicorn[standard]==0.49.0
httpx==0.28.1
python-dotenv==1.2.2
pydantic==2.13.4
pyyaml==6.0.3

# the workforce (agents the BFF spawns)
band-sdk[langgraph]==1.0.0
langchain-openai==1.3.0
langchain-core==1.4.7
langgraph==1.2.5
openai==2.41.1
PyGithub==2.9.1

# scanner CLIs the scanners shell out to
bandit==1.9.4
pip-audit
detect-secrets==1.5.0
